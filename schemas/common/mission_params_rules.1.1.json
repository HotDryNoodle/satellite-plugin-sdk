{
  "$comment": "SSOT for Mission params required/forbidden by task_type. DO NOT duplicate in C++ or envelope allOf. Run: python3 scripts/generate_mission_params_rules.py → writes mission_params_by_task_type.1.1.generated.schema.json (envelope $ref) and src/satellite/mission_params_rules.generated.hpp (runtime). Structural shapes: receiver/time_range/geo_point schemas.",
  "schema_version": "1.1",
  "by_task_type": {
    "historical_replay": {
      "required": ["receiver", "time_range"],
      "forbidden": ["target_lla", "execute_ip"]
    },
    "remote_detection": {
      "required": ["target_lla", "receiver", "time_range"],
      "forbidden": ["execute_ip"]
    },
    "single_remote_detection": {
      "required": ["target_lla", "receiver", "time_range", "execute_ip"],
      "forbidden": []
    }
  }
}
